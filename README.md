Arguments

--input_folder: Path to the folder containing images to pixelate (required).
--output_folder: Path to the output folder for pixelated images (required).
--pixel_size: Pixelation factor (default: 16). Higher values create more pixelated effects.
